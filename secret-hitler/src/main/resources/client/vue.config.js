module.exports = {
    // This will export the production build right into the public folder in resources
    // So the client application can be served with Javalin static file context
    outputDir: "../public"
}